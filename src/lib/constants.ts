export const LOYALTY_META_PADRAO = 10
export const CLIENTE_SUMINDO_DIAS = 45
/** Nome da plataforma em si (multi-tenant) — usado só nas telas
 * compartilhadas por todas as barbearias (login/cadastro, título da aba,
 * PWA). Dentro do painel já logado, cada tela mostra o nome da barbearia
 * de quem está logado (vindo do banco), não esse aqui. */
export const NOME_PLATAFORMA = 'NexoBarber'
